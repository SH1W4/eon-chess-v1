[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v
markers =
    integration: Tests that integrate multiple components
    arquimax: Tests for the ARQUIMAX system
    nexus: Tests for the NEXUS system
